\documentclass[10pt]{article}

% =========================================================
% PAGE SETUP
% =========================================================
% FINAL ZINE PAGE SIZE
%
% This sets the physical page dimensions for the compiled page.
% Coordinates for content boxes are measured in inches from the
% UPPER-LEFT corner of the page.
%
% If your final zine page is 5.25in x 4.25in, define that HERE
% so font sizes and layout are tuned to the real output size.
\usepackage[
  paperwidth=5.25in,
  paperheight=4.25in,
  margin=0in
]{geometry}
\pagestyle{empty}

% =========================================================
% CORE PACKAGES
% =========================================================
% textpos   = absolute positioning of text boxes
% tcolorbox = visible content boxes with padding/borders
% graphicx  = rotation support
% xparse    = cleaner custom command definitions
% pgfkeys   = key/value settings for content boxes
\usepackage[absolute,overlay]{textpos}
\usepackage[most]{tcolorbox}
\usepackage{graphicx}
\usepackage{xparse}
\usepackage{pgfkeys}

% =========================================================
% TYPOGRAPHY / CONTENT PACKAGES
% =========================================================
% Requires XeLaTeX or LuaLaTeX
%
% fontspec   = system/OpenType font support
% microtype  = subtle spacing improvements
% ragged2e   = better ragged-right text
% enumitem   = bullet/numbered list spacing control
% titlesec   = heading style control
% hyperref   = link/bookmark support
% longtable  = tables generated by pandoc
% booktabs   = table rules generated by pandoc
% array      = extra table formatting
% amssymb    = defines \square, which pandoc may use for checkboxes
\usepackage{fontspec}
\usepackage{microtype}
\usepackage{ragged2e}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{hyperref}
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{array}
\usepackage{amssymb}

% =========================================================
% GLOBAL POSITIONING GRID
% =========================================================
% Coordinates are specified in inches.
%
% Example:
% x=0.15, y=0.15
% means 0.15 inches from left edge and 0.15 inches from top edge.
\setlength{\TPHorizModule}{1in}
\setlength{\TPVertModule}{1in}

% =========================================================
% FONT SELECTION
% =========================================================
% Main body font
\setmainfont{TeX Gyre Heros}[Ligatures=TeX]

% Sans font
\setsansfont{TeX Gyre Heros}[Ligatures=TeX]

% Monospace font
% Useful for callsigns, frequencies, code-like text, etc.
\setmonofont{TeX Gyre Cursor}[Scale=0.90]

% =========================================================
% GLOBAL FONT SIZE DEFINITIONS
% =========================================================
% Keep font sizes HERE so they are easy to tune later.
%
% BODY TEXT:
% Main text size used inside content boxes
\newcommand{\BodyFont}{\fontsize{9pt}{10.8pt}\selectfont}

% SECTION HEADINGS:
% Markdown "#" heading
\newcommand{\SectionFont}{\bfseries\fontsize{14pt}{15.5pt}\selectfont}

% Markdown "##" heading
\newcommand{\SubsectionFont}{\bfseries\fontsize{11pt}{12.5pt}\selectfont}

% Markdown "###" heading
\newcommand{\SubsubsectionFont}{\bfseries\fontsize{9.8pt}{11pt}\selectfont}

% =========================================================
% BODY TEXT / PARAGRAPH TUNING
% =========================================================
% No paragraph indent
\setlength{\parindent}{0pt}

% Space between paragraphs
\setlength{\parskip}{3pt}

% Slightly loosen line spacing for readability
\linespread{1.02}

% Helps reduce ugly overfull lines
\emergencystretch=1em

% Better for narrow text boxes than full justification
\RaggedRight

% Pandoc helper: prevents errors with compact lists
\providecommand{\tightlist}{%
  \setlength{\itemsep}{1pt}%
  \setlength{\parskip}{0pt}%
}

% =========================================================
% LIST STYLING
% =========================================================
% These settings affect Markdown bullet lists and numbered lists.
\setlist[itemize]{
  leftmargin=1.15em,
  itemsep=1.5pt,
  topsep=1.5pt,
  parsep=0pt,
  partopsep=0pt
}
\setlist[enumerate]{
  leftmargin=1.30em,
  itemsep=1.5pt,
  topsep=1.5pt,
  parsep=0pt,
  partopsep=0pt
}

% Bullet symbol for unordered lists
\renewcommand{\labelitemi}{\textendash}

% =========================================================
% HEADING STYLING
% =========================================================
% This controls Markdown heading appearance after Pandoc conversion.
%
% "#" in Markdown
\titleformat{\section}
  {\SectionFont}
  {}{0pt}{}

% "##" in Markdown
\titleformat{\subsection}
  {\SubsectionFont}
  {}{0pt}{}

% "###" in Markdown
\titleformat{\subsubsection}
  {\SubsubsectionFont}
  {}{0pt}{}

% Spacing around headings
\titlespacing*{\section}{0pt}{5pt}{2pt}
\titlespacing*{\subsection}{0pt}{4pt}{1pt}
\titlespacing*{\subsubsection}{0pt}{3pt}{1pt}

% =========================================================
% HYPERLINK SETTINGS
% =========================================================
% Keep links invisible in print/PDF output.
\hypersetup{
  hidelinks,
  pdfauthor={},
  pdftitle={}
}

% =========================================================
% DIRECTORIES
% =========================================================
% This is where Pandoc-generated .tex fragments live.
% Content boxes will pull files from this folder.
\newcommand{\GeneratedDir}{pages/generated/}

% =========================================================
% DEFAULT BOX STYLE
% =========================================================
% These are fallback settings used by content boxes.
%
% Default border thickness
% Set rule=0pt in a box definition for NO border.
\newcommand{\DefaultBoxRule}{0.4pt}

% Corner rounding radius
% Set to 0pt for perfectly square corners.
\newcommand{\DefaultArc}{1pt}

% Default border color
\newcommand{\DefaultBorderColor}{black}

% Default content padding
\newcommand{\DefaultHPad}{6pt}
\newcommand{\DefaultVPad}{5pt}

% =========================================================
% CONTENT BOX USER-EDITABLE KEYS
% =========================================================
% This is where the available box settings are defined.
%
% x           = left position in inches
% y           = top position in inches
% width       = box width in inches
% height      = box height in inches
% hpad        = left/right padding inside box
% vpad        = top/bottom padding inside box
% angle       = rotation in degrees
% bordercolor = border color
% rule        = border thickness (set to 0pt for no border)
% filename    = generated .tex file to place in the box
\pgfkeys{
/contentbox/.is family,
/contentbox,
x/.initial=0,
y/.initial=0,
width/.initial=1,
height/.initial=1,
hpad/.initial=\DefaultHPad,
vpad/.initial=\DefaultVPad,
angle/.initial=0,
bordercolor/.initial=\DefaultBorderColor,
rule/.initial=\DefaultBoxRule,
filename/.initial=
}

% =========================================================
% INTERNAL CONTENT BOX FUNCTION
% =========================================================
% This is the messy machinery.
%
% In normal use, you should NOT edit this section unless
% you are changing how the box system fundamentally works.
%
% This function:
% 1. reads the box settings
% 2. places the box at the specified coordinates
% 3. rotates it if requested
% 4. draws the tcolorbox
% 5. inserts the generated .tex content file
\NewDocumentCommand{\ContentBox}{m}{%
  \pgfkeys{/contentbox/.cd,#1}%
  \begin{textblock*}{\pgfkeysvalueof{/contentbox/width}in}
    (\pgfkeysvalueof{/contentbox/x}in,\pgfkeysvalueof{/contentbox/y}in)
    \rotatebox[origin=c]{\pgfkeysvalueof{/contentbox/angle}}{%
      \begin{tcolorbox}[
        width=\pgfkeysvalueof{/contentbox/width}in,
        height=\pgfkeysvalueof{/contentbox/height}in,
        colback=white,
        colframe=\pgfkeysvalueof{/contentbox/bordercolor},
        boxrule=\pgfkeysvalueof{/contentbox/rule},
        arc=\DefaultArc,
        left=\pgfkeysvalueof{/contentbox/hpad},
        right=\pgfkeysvalueof{/contentbox/hpad},
        top=\pgfkeysvalueof{/contentbox/vpad},
        bottom=\pgfkeysvalueof{/contentbox/vpad},
        valign=top,
        halign=flush left,
        sharp corners,
        enhanced
      ]
      \BodyFont
      \input{\GeneratedDir\pgfkeysvalueof{/contentbox/filename}}
      \end{tcolorbox}
    }%
  \end{textblock*}
}

% =========================================================
% DOCUMENT CONTENT
% =========================================================
% This is where you define textbox size, location, rotation,
% border, and which content file gets placed in each box.
%
% Notes:
% - width/height/x/y are in inches
% - angle is in degrees
% - rule=0pt means NO border
% - filename should point to a generated .tex file
%   in pages/generated/
%
% For a full-page content box on this zine page, use something around:
% x=0.15, y=0.15, width=4.95, height=3.95
%
% The examples below split the page into two stacked boxes.
\begin{document}

\ContentBox{
x=0.15,
y=0.15,
width=4.95,
height=1.75,
hpad=6pt,
vpad=5pt,
angle=0,
rule=0.4pt,
filename=checklist.tex
}

\ContentBox{
x=0.15,
y=2.00,
width=4.95,
height=2.10,
hpad=6pt,
vpad=5pt,
angle=0,
rule=0.4pt,
filename=frequencies.tex
}

\end{document}
